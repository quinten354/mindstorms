# import modules
import hub

class center:
    is_pressed = hub.button.center.is_pressed
    was_pressed = hub.button.center.was_pressed
    presses = hub.button.center.presses

class connect:
    is_pressed = hub.button.connect.is_pressed
    was_pressed = hub.button.connect.was_pressed
    presses = hub.button.connect.presses

class left:
    is_pressed = hub.button.left.is_pressed
    was_pressed = hub.button.left.was_pressed
    presses = hub.button.left.presses

class right:
    is_pressed = hub.button.right.is_pressed
    was_pressed = hub.button.right.was_pressed
    presses = hub.button.right.presses

